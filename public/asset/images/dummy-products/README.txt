Homepage product images are intentionally local dummy images for presentation.
To switch the homepage back to API-provided product images, create/update the project .env file with:

VITE_HOME_DUMMY_IMAGES=false

Then restart the Vite dev server.
