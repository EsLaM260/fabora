import {Minus,Plus,Trash2} from 'lucide-react';import {money} from '../../utils/format';import {useTranslation} from 'react-i18next';
export default function CartItem({item,onChange,onRemove}:{item:any;onChange:(q:number)=>void;onRemove:()=>void}){const {t}=useTranslation();return <div className="grid grid-cols-[110px_1fr_auto] gap-5 py-5 border-b thin-border"><img src={item.imageUrl||item.image} alt={item.productTitle||item.name||t('common.product')} className="w-[110px] aspect-[3/4] object-cover bg-[#ece8e2]"/><div><h3 className="text-sm font-medium">{item.productTitle||item.name}</h3><p className="text-xs text-muted mt-1">{item.color||t('product.defaultColor')} · {t('common.size')} {item.size||'M'}</p><div className="flex items-center border thin-border w-fit mt-6"><button aria-label={t('common.decrease')} onClick={()=>onChange(Math.max(1,item.quantity-1))} className="p-2"><Minus size={13}/></button><span className="w-8 text-center text-xs">{item.quantity}</span><button aria-label={t('common.increase')} onClick={()=>onChange(item.quantity+1)} className="p-2"><Plus size={13}/></button></div></div><div className="text-right"><p className="text-sm">{money(item.finalLineTotal??item.unitPrice*item.quantity,'EGP')}</p><button aria-label={t('common.remove')} onClick={onRemove} className="mt-5 text-muted"><Trash2 size={15}/></button></div></div>}
