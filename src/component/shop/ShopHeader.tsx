import {Search,X,SlidersHorizontal} from 'lucide-react';
import {useTranslation} from 'react-i18next';
export default function ShopHeader({search,setSearch,onFilter,sortOrder,onSort}:{search:string;setSearch:(v:string)=>void;onFilter:()=>void;sortOrder:string;onSort:(v:string)=>void}){
 const {t}=useTranslation();
 return <section className="w-full px-5 md:px-8 pt-14 pb-8"><div className="flex flex-col md:flex-row gap-6 justify-between md:items-end"><div><div className="text-[10px] uppercase tracking-[.2em] text-muted">fabora / {t('common.shop').toUpperCase()}</div><h1 className="serif text-5xl mt-2">{t('shop.title')}</h1></div><div className="w-full md:w-80 border thin-border bg-white flex items-center px-3"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={t('shop.search')} className="outline-none flex-1 px-3 py-3 text-sm bg-transparent"/><button onClick={()=>setSearch('')} aria-label={t('common.clear')}><X size={15}/></button></div></div><div className="mt-10 pt-5 border-t thin-border flex justify-between items-center"><button onClick={onFilter} className="flex items-center gap-2 text-xs uppercase tracking-[.14em]"><SlidersHorizontal size={15}/>{t('shop.filter')}</button><select value={sortOrder} onChange={e=>onSort(e.target.value)} className="bg-transparent text-xs uppercase tracking-[.14em] outline-none"><option value="desc">{t('shop.newest')}</option><option value="asc">{t('shop.oldest')}</option></select></div></section>
}
