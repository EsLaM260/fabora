import { useNavigate } from 'react-router-dom';
import ProductCard from '../common/ProductCard';
export default function RelatedProductsSection({ product, products }: { product: any; products: any[] }) { const nav = useNavigate(); return <section className="w-full px-5 md:px-8 pb-16"><div className="flex justify-between items-end mb-7"><h2 className="serif text-3xl">You may also like</h2><button onClick={() => nav('/shop')} className="text-xs uppercase tracking-[.14em]">View all →</button></div><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{products.filter(p => p.id !== product.id).slice(0, 4).map(p => <ProductCard key={p.id} product={p} />)}</div></section> }
