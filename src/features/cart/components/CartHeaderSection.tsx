import { useTranslation } from 'react-i18next';
export default function CartHeaderSection({count}:{count:number}){const {t}=useTranslation();return <section className="w-full px-5 md:px-8 py-10"><div className="flex justify-between items-end"><div><div className="text-[10px] uppercase tracking-[.2em] text-muted">fabora / {t('cart.bag')}</div><h1 className="serif text-5xl mt-2">{t('cart.bag')}</h1></div><span className="text-xs text-muted">{count} {t('common.items')}</span></div></section>}
